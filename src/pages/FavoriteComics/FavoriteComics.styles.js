import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        flex:1
    },
    lottie: {
        width: '100%',
        aspectRatio: 1,
        alignItems:'center'
    }
});
